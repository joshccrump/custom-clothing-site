export async function loadClients(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error('Failed');return await r.json()} export function filterClients(cs,q=''){q=(q||'').toLowerCase().trim();return cs.filter(c=>{const hay=[c.name||'',c.slug||'',c.notes||'',...(c.tags||[])].join(' ').toLowerCase();return q?hay.includes(q):true})}