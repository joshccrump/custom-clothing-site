# Square API credentials
# Copy to `.env.local` for local development, or provide the same
# variables via your hosting provider / CI secrets settings.
SQUARE_ENVIRONMENT=production
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
SQUARE_APP_ID=
