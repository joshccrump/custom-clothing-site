{
  "name": "custom-clothing-site",
  "version": "0.1.3",
  "private": true,
  "description": "Static site with Square catalog sync for products.json",
  "scripts": {
    "preflight:square": "node scripts/preflight.mjs",
    "sync:square": "node scripts/fetch-square.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
